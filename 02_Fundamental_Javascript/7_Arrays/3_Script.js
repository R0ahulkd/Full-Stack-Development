arr = [1,2,3,4,5];
arr1 = [6,7,8,9,10];

let new_arr = arr.concat(arr1); // Concatenate 2 Arrays
console.log(new_arr);

console.log(arr1.join("-")); //change array into string with - (if we remain blank then it joins with comma